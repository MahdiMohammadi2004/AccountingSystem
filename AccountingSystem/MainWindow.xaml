<Window x:Class="AccountingSystem.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="????? ?????? ???? ? ????????" Height="700" Width="1200"
        WindowState="Maximized"
        Background="#F5F5F5">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Menu -->
        <Menu Grid.Row="0" Background="#333">
            <MenuItem Header="???????" Click="Dashboard_Click" Foreground="White" FontSize="14"/>
            <MenuItem Header="?????? ?????" Click="GeneralBalance_Click" Foreground="White" FontSize="14"/>
            <MenuItem Header="??????????" Click="Journal_Click" Foreground="White" FontSize="14"/>
            <MenuItem Header="???? ???????" Click="Customers_Click" Foreground="White" FontSize="14"/>
            <MenuItem Header="????????? ? ????" Click="Shipments_Click" Foreground="White" FontSize="14"/>
            <MenuItem Header="????????" Click="Reports_Click" Foreground="White" FontSize="14"/>
            <MenuItem Header="???" Click="Print_Click" Foreground="White" FontSize="14"/>
            <MenuItem Header="?????" Click="Backup_Click" Foreground="White" FontSize="14"/>
            <MenuItem Header="????" Click="Exit_Click" Foreground="White" FontSize="14" HorizontalAlignment="Right"/>
        </Menu>
        
        <!-- Content Area -->
        <Frame x:Name="MainFrame" Grid.Row="1" NavigationUIVisibility="Hidden"/>
    </Grid>
</Window>
