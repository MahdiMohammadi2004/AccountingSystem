<Page x:Class="FinancialSystem.GeneralBalancePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="General Balance">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0" Text="General Balance" FontSize="24" FontWeight="Bold" 
                   Margin="0,0,0,20" HorizontalAlignment="Center"/>
        
        <!-- Add Transaction Form -->
        <Border Grid.Row="1" Background="White" CornerRadius="10" Padding="20" Margin="0,0,0,20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <TextBlock Grid.Row="0" Grid.Column="0" Text="Date:" Margin="0,0,0,5"/>
                <DatePicker x:Name="dpTransactionDate" Grid.Row="1" Grid.Column="0" Margin="0,0,0,10"/>
                
                <TextBlock Grid.Row="0" Grid.Column="1" Text="Transaction Type:" Margin="0,0,0,5"/>
                <ComboBox x:Name="cmbTransactionType" Grid.Row="1" Grid.Column="1" Margin="0,0,0,10">
                    <ComboBoxItem Content="Income" IsSelected="True"/>
                    <ComboBoxItem Content="Expense"/>
                </ComboBox>
                
                <TextBlock Grid.Row="2" Grid.Column="0" Text="Amount:" Margin="0,0,0,5"/>
                <TextBox x:Name="txtAmount" Grid.Row="3" Grid.Column="0" Margin="0,0,0,10"/>
                
                <TextBlock Grid.Row="2" Grid.Column="1" Text="Account Name:" Margin="0,0,0,5"/>
                <TextBox x:Name="txtAccountName" Grid.Row="3" Grid.Column="1" Margin="0,0,0,10"/>
                
                <TextBlock Grid.Row="4" Grid.Column="0" Text="Document Number:" Margin="0,0,0,5"/>
                <TextBox x:Name="txtDocumentNumber" Grid.Row="5" Grid.Column="0" Margin="0,0,0,10"/>
                
                <TextBlock Grid.Row="4" Grid.Column="1" Text="Details:" Margin="0,0,0,5"/>
                <TextBox x:Name="txtDetails" Grid.Row="5" Grid.Column="1" Margin="0,0,0,10"/>
                
                <Button Grid.Row="6" Grid.Column="0" Content="Add Transaction" Width="120" Height="35" 
                        Click="BtnAddTransaction_Click" HorizontalAlignment="Left" 
                        Background="#27AE60" Foreground="White" BorderThickness="0"/>
            </Grid>
        </Border>
        
        <!-- Transactions List -->
        <Border Grid.Row="2" Background="White" CornerRadius="10" Padding="20">
            <DataGrid x:Name="dgTransactions" AutoGenerateColumns="False" 
                      CanUserAddRows="False" IsReadOnly="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Date" Binding="{Binding TransactionDate}" Width="100"/>
                    <DataGridTextColumn Header="Details" Binding="{Binding Details}" Width="200"/>
                    <DataGridTextColumn Header="Type" Binding="{Binding Type}" Width="80"/>
                    <DataGridTextColumn Header="Amount" Binding="{Binding Amount}" Width="100"/>
                    <DataGridTextColumn Header="Account" Binding="{Binding AccountName}" Width="120"/>
                    <DataGridTextColumn Header="Doc Number" Binding="{Binding DocumentNumber}" Width="100"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</Page>
